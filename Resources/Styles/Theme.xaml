<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!--
    ╔══════════════════════════════════════════════════════════════════════╗
    ║  THEME 2.0 — "Deep Field"                                              ║
    ║  Premium dark design system, Windows 11 native (Mica-aware).          ║
    ║  Saha kullanımı için göz yormayan derin koyu yüzeyler; elektrik mavisi ║
    ║  vurgu; yumuşak gölge ile derinlik; hover/press mikro-etkileşimleri.   ║
    ║  Mevcut tüm anahtarlar korunur — sayfalar değişmeden modernleşir.      ║
    ╚══════════════════════════════════════════════════════════════════════╝
    -->

    <!-- ════════ SURFACE HIERARCHY (hafif mavi alt-ton ile derinlik) ════════ -->
    <Color x:Key="ColorPageBg">#0A0B0F</Color>          <!-- en derin zemin -->
    <Color x:Key="ColorSurface">#15161C</Color>          <!-- kart / konteyner -->
    <Color x:Key="ColorSurfaceHigh">#1F2128</Color>      <!-- yükseltilmiş yüzey, input -->
    <Color x:Key="ColorSurfaceSubtle">#101117</Color>    <!-- ince zemin -->
    <Color x:Key="ColorSurfaceGlass">#CC181A22</Color>   <!-- yarı saydam cam yüzey -->

    <!-- Mica'nın görüneceği yarı saydam flyout zemini (yoksa koyu kalır) -->
    <Color x:Key="ColorFlyoutBg">#D9111219</Color>

    <!-- ════════ BORDERS & DIVIDERS ════════ -->
    <Color x:Key="ColorBorder">#2A2C36</Color>
    <Color x:Key="ColorBorderStrong">#3A3D49</Color>
    <Color x:Key="ColorBorderSubtle">#20222B</Color>

    <!-- ════════ TEXT ════════ -->
    <Color x:Key="ColorTextPrimary">#FFFFFF</Color>
    <Color x:Key="ColorTextSecondary">#AEB1BC</Color>
    <Color x:Key="ColorTextTertiary">#7E828F</Color>

    <!-- ════════ BRAND & SEMANTIC ════════ -->
    <Color x:Key="ColorAccent">#0A84FF</Color>           <!-- elektrik mavisi -->
    <Color x:Key="ColorAccentSoft">#2A9BFF</Color>
    <Color x:Key="ColorAccentMuted">#1E3A5F</Color>       <!-- vurgu tint (seçili zemin) -->
    <Color x:Key="ColorIndigo">#5E5CE6</Color>
    <Color x:Key="ColorViolet">#BF5AF2</Color>
    <Color x:Key="ColorSuccess">#30D158</Color>
    <Color x:Key="ColorSuccessMuted">#16321F</Color>
    <Color x:Key="ColorWarning">#FF9F0A</Color>
    <Color x:Key="ColorDanger">#FF453A</Color>
    <Color x:Key="ColorRecord">#FF3B30</Color>
    <Color x:Key="ColorHighlight">#FFD60A</Color>
    <Color x:Key="ColorCyan">#64D2FF</Color>

    <!-- ════════ BRUSHES (Shape uyumlu) ════════ -->
    <SolidColorBrush x:Key="BrushPageBg" Color="{StaticResource ColorPageBg}" />
    <SolidColorBrush x:Key="BrushSurface" Color="{StaticResource ColorSurface}" />
    <SolidColorBrush x:Key="BrushSurfaceHigh" Color="{StaticResource ColorSurfaceHigh}" />
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}" />
    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}" />

    <!-- ════════ GRADIENTS ════════ -->
    <LinearGradientBrush x:Key="GradientAccent" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#3D9DFF" Offset="0" />
        <GradientStop Color="#0A84FF" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="GradientBrand" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#0A84FF" Offset="0" />
        <GradientStop Color="#5E5CE6" Offset="0.55" />
        <GradientStop Color="#BF5AF2" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="GradientDanger" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#FF5A4F" Offset="0" />
        <GradientStop Color="#E0352B" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="GradientSuccess" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#3BDC66" Offset="0" />
        <GradientStop Color="#24B84C" Offset="1" />
    </LinearGradientBrush>

    <!-- Kart tepesine ince ışık vuruşu (derinlik hissi) -->
    <LinearGradientBrush x:Key="GradientSurface" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#1C1E26" Offset="0" />
        <GradientStop Color="#15161C" Offset="1" />
    </LinearGradientBrush>

    <!-- Video tuval zemini -->
    <LinearGradientBrush x:Key="GradientVideoVoid" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#0D0F14" Offset="0" />
        <GradientStop Color="#050608" Offset="1" />
    </LinearGradientBrush>

    <!-- ════════ SPACING SCALE ════════ -->
    <x:Double x:Key="SpaceXS">4</x:Double>
    <x:Double x:Key="SpaceS">8</x:Double>
    <x:Double x:Key="SpaceM">12</x:Double>
    <x:Double x:Key="SpaceL">16</x:Double>
    <x:Double x:Key="SpaceXL">24</x:Double>
    <x:Double x:Key="Space2XL">32</x:Double>

    <!-- ════════ RADIUS SCALE ════════ -->
    <x:Double x:Key="RadiusInput">10</x:Double>
    <x:Double x:Key="RadiusButton">12</x:Double>
    <x:Double x:Key="RadiusCard">16</x:Double>
    <x:Double x:Key="RadiusPill">22</x:Double>

    <!-- ════════ SHADOWS ════════ -->
    <!-- NOT: MAUI'de Shadow : Element. Tek bir Shadow örneği birden çok view'a
         paylaştırılırsa (stil setter'ı üzerinden) yalnızca sonuncuda görünür.
         Bu yüzden gölge yalnızca TEKİL hero elemanlarda kullanılır; her biri
         aşağıdaki kaynağı yalnızca BİR kez referanslar (ya da inline tanımlanır). -->
    <Shadow x:Key="ShadowDanger" Brush="#FF453A" Offset="0,4" Radius="18" Opacity="0.50" />

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!--  CARDS                                                                -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="CardBorder" TargetType="Border">
        <Setter Property="Stroke" Value="{StaticResource ColorBorder}" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource GradientSurface}" />
        <Setter Property="StrokeShape" Value="RoundRectangle 16" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <Style x:Key="CardBorderAccent" TargetType="Border" BasedOn="{StaticResource CardBorder}">
        <Setter Property="Stroke" Value="{StaticResource ColorSuccess}" />
        <Setter Property="StrokeThickness" Value="1.5" />
    </Style>

    <!-- Yarı saydam cam kart (Mica üzerinde) -->
    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Stroke" Value="{StaticResource ColorBorderStrong}" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource ColorSurfaceGlass}" />
        <Setter Property="StrokeShape" Value="RoundRectangle 16" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <!-- İnce ayırıcı -->
    <Style x:Key="Divider" TargetType="BoxView">
        <Setter Property="HeightRequest" Value="1" />
        <Setter Property="Color" Value="{StaticResource ColorBorderSubtle}" />
    </Style>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!--  TYPOGRAPHY SCALE                                                     -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="Display" TargetType="Label">
        <Setter Property="FontSize" Value="30" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
    </Style>

    <Style x:Key="Title" TargetType="Label">
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
    </Style>

    <Style x:Key="Subtitle" TargetType="Label">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextSecondary}" />
    </Style>

    <!-- Section title (küçük, harf aralıklı üst başlık) -->
    <Style x:Key="SectionTitle" TargetType="Label">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextTertiary}" />
        <Setter Property="CharacterSpacing" Value="1.4" />
        <Setter Property="Margin" Value="0,0,0,4" />
    </Style>

    <!-- Overline (en küçük etiket) -->
    <Style x:Key="Overline" TargetType="Label">
        <Setter Property="FontSize" Value="9" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextTertiary}" />
        <Setter Property="CharacterSpacing" Value="1.6" />
    </Style>

    <Style x:Key="CardTitle" TargetType="Label">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
    </Style>

    <Style x:Key="BodyText" TargetType="Label">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
    </Style>

    <Style x:Key="Caption" TargetType="Label">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextSecondary}" />
    </Style>

    <!-- İkon (Segoe Fluent / MDL2 glyph) etiketi. Windows sistem fontu — kayıt gerekmez. -->
    <Style x:Key="Glyph" TargetType="Label">
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!--  BUTTONS — hover/press mikro-etkileşimli                              -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorAccent}" />
        <Setter Property="TextColor" Value="White" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="HeightRequest" Value="46" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="1" />
                            <Setter Property="Opacity" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="1.025" />
                            <Setter Property="Opacity" Value="0.92" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="0.97" />
                            <Setter Property="Opacity" Value="0.85" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity" Value="0.4" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSuccess}" />
        <Setter Property="TextColor" Value="#06210E" />
    </Style>

    <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorWarning}" />
        <Setter Property="TextColor" Value="#3A2400" />
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorDanger}" />
        <Setter Property="TextColor" Value="White" />
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="BorderColor" Value="{StaticResource ColorBorderStrong}" />
        <Setter Property="BorderWidth" Value="1" />
        <Setter Property="HeightRequest" Value="42" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontAttributes" Value="None" />
    </Style>

    <Style x:Key="QuietButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextSecondary}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="HeightRequest" Value="36" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontAttributes" Value="None" />
    </Style>

    <!-- Sticky acil durdurma — gradient + güçlü kırmızı gölge -->
    <Style x:Key="EmergencyStopButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GradientDanger}" />
        <Setter Property="TextColor" Value="White" />
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="HeightRequest" Value="58" />
        <Setter Property="FontSize" Value="17" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="Shadow" Value="{StaticResource ShadowDanger}" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="1.02" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="0.96" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <!-- Yuvarlak ikon buton (40x40) -->
    <Style x:Key="IconButton" TargetType="Button">
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="BorderColor" Value="{StaticResource ColorBorderStrong}" />
        <Setter Property="BorderWidth" Value="1" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="WidthRequest" Value="42" />
        <Setter Property="HeightRequest" Value="42" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="1.06" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Scale" Value="0.92" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!--  CHIPS & BADGES                                                       -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="Chip" TargetType="Border">
        <Setter Property="Stroke" Value="{StaticResource ColorBorderStrong}" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="StrokeShape" Value="RoundRectangle 20" />
        <Setter Property="Background" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="Padding" Value="12,6" />
    </Style>

    <Style x:Key="ChipAccent" TargetType="Border" BasedOn="{StaticResource Chip}">
        <Setter Property="Stroke" Value="{StaticResource ColorAccent}" />
        <Setter Property="Background" Value="{StaticResource ColorAccentMuted}" />
    </Style>

    <!-- Küçük durum rozeti (pill) -->
    <Style x:Key="Badge" TargetType="Border">
        <Setter Property="StrokeThickness" Value="0" />
        <Setter Property="StrokeShape" Value="RoundRectangle 10" />
        <Setter Property="Background" Value="{StaticResource ColorAccentMuted}" />
        <Setter Property="Padding" Value="8,3" />
        <Setter Property="HorizontalOptions" Value="Start" />
    </Style>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!--  FORM INPUTS                                                          -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <Style TargetType="Entry">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="PlaceholderColor" Value="{StaticResource ColorTextTertiary}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="HeightRequest" Value="42" />
    </Style>

    <Style TargetType="Picker">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="TitleColor" Value="{StaticResource ColorTextTertiary}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="HeightRequest" Value="42" />
    </Style>

    <Style TargetType="Editor">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="PlaceholderColor" Value="{StaticResource ColorTextTertiary}" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="DatePicker">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="TimePicker">
        <Setter Property="BackgroundColor" Value="{StaticResource ColorSurfaceHigh}" />
        <Setter Property="TextColor" Value="{StaticResource ColorTextPrimary}" />
        <Setter Property="FontSize" Value="13" />
    </Style>

</ResourceDictionary>
